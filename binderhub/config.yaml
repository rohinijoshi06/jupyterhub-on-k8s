jupyterhub:
  hub:
    baseUrl: /jupyterhub/

# BinderHub config
config:
  BinderHub:
    use_registry: true
    image_prefix: srcdev/binder-dev-
    hub_url: http://<binderhub_ip>/jupyterhub/
    base_url: /binderhub/

service:
  type: NodePort